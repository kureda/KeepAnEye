<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".carer.ui.CarerMainActivity">

    <android.support.design.widget.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/InstallationTheme.AppBarOverlay">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            app:popupTheme="@style/InstallationTheme.PopupOverlay"
            app:titleTextAppearance="@style/Toolbar.TitleText" />
    </android.support.design.widget.AppBarLayout>

    <com.kureda.android.keepaneye.both.util.FontTextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:drawableEnd="@drawable/ic_shadow"
        android:drawableRight="@drawable/ic_shadow"
        android:layout_gravity="bottom|end"
        android:gravity="bottom|end"
        android:textColor="@color/blue100"
        android:text="@string/background_text"
        android:textSize="56sp"
        android:padding="30dp"
        />

    <include layout="@layout/content_carer_main" />

</android.support.design.widget.CoordinatorLayout>
